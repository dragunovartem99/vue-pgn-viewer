name: Release

on:
    push:
        branches: ["main"]

permissions:
    contents: write
    pull-requests: write
    id-token: write

jobs:
    ci:
        uses: ./.github/workflows/ci.yaml

    pipes:
        needs: ci
        uses: dragunovartem99/pipes/.github/workflows/release-npm.yaml@v1.2.0
